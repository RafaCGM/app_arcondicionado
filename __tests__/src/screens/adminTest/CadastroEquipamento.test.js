import React from 'react';
import { render, fireEvent, waitFor, screen } from '@testing-library/react-native';
import ConfigScreen from '../../../../src/screens/admin/equipamento/CadastroEquipamentoScreen';

test('dummy test', () => {
  expect(true).toBe(true);
});